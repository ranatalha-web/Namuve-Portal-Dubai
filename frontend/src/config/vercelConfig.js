/**
 * Vercel Configuration
 * Define the backend URL to use when the app is deployed on Vercel.
 */

const VERCEL_BACKEND_URL = 'https://uae.portal.namuve.com'; // Uses HTTPS to avoid Mixed Content errors

export default VERCEL_BACKEND_URL;
